<h1>Edit a Kitten</h1>

<%= render 'form' %>
  
<%= link_to "Delete #{@kitten.name}",  kitten_path(@kitten), 
                              method: :delete, 
                              data: { confirm: 'Are you sure?'} %>